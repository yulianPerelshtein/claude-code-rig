# domains/testing-tdd — README

| File | Lesson |
|---|---|
| `pytest-mocking.md` | patched `os.path.exists` breaks `makedirs`; MagicMock identity; mock as context manager. |
| `ai-judge-parser-caller-split.md` | split pure JSON parser from the LLM call for testability. |
| `verifying-the-test.md` | mutation-test a regression test; never mask a failing one. |
