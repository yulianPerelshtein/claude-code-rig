# domains/cloud-aws — README

| File | Lesson |
|---|---|
| `sso-and-profiles.md` | `AWS_PROFILE=<profile>` needed for boto3 AND the CLI after SSO login. |
| `eventbridge-patterns.md` | validate `EventPattern` offline before deploy. |
| `cloudformation.md` | explicit `DependsOn` for Rule → Logs ResourcePolicy. |
| `fargate-cache-patterns.md` | ephemeral-volume asset cache; path-traversal guard; try/except scope. |
