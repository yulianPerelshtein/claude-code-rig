---
name: cloud-aws
description: >-
  AWS operational patterns: SSO profile gotchas (boto3 and CLI), EventBridge
  rule pattern validation, CloudFormation dependency ordering, and Fargate
  ephemeral-volume asset caching. Use for AWS CLI, boto3, CloudFormation, or
  EventBridge work.
paths:
  - "**/*.tf"
  - "**/template*.y*ml"
  - "**/cloudformation/**"
  - "**/*.cfn.*"
---

# cloud-aws

AWS knowledge, project-agnostic. See `README.md` for the index. Profile names
are placeholders (`<profile>`) — set per environment.
