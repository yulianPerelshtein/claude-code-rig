---
name: scraping
description: >-
  Browser automation and web scraping for agents — the official Playwright MCP
  as the default driver. Use when a task needs to drive a real browser, scrape a
  rendered page, or automate a web UI.
---

# scraping

Default browser/automation tooling for the rig. Read `playwright-mcp.md` for the
Playwright MCP server (the locked default per `SOTA_REFRESH.md §4.2`).

Escalation path (not installed by default): **Scrapling** for anti-bot/stealth
scraping (Tier 2, `ENHANCEMENTS_BACKLOG.md §3.3`); **obscura** stays watch-list
only. Reach for those only when Playwright MCP demonstrably can't get the page.
