# domains/devops — README

| File | Lesson |
|---|---|
| `git-conventions.md` | branch preflight; verify base before extracting a branch. |
| `pr-cleanliness.md` | zero blank-line noise; the baseline technique; bidirectional noise. |
| `github-cli.md` | use REST (not `gh pr edit`) for PR body/title updates. |
| `ci-llm-key-defense.md` | set the LLM key to `""` in the CI job env block. |
| `gh-account-vs-git-identity.md` | `gh`'s active account ≠ git's SSH identity; never `gh auth switch` to fix a lookup. |
