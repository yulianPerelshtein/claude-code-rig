# Repo onboarding — <project-name>

A short orientation to fill in when first working in a repo. Goal: enough to act
safely, not an exhaustive doc.

## What it is

- Purpose / domain (1–2 lines).
- Primary language(s), framework(s), runtime.

## How to run

- Install: `<cmd>`
- Run locally: `<cmd>` (Docker? services? env vars?)
- Tests: `<cmd>`  · Lint: `<cmd>`  · Build: `<cmd>`

## Where things live

- Entry point: `<file>`
- **Primary service / business-logic file**: `<file>`  (agents miss this — name it)
- Config / settings: `<file>`
- Tests: `<dir>`

## Conventions

- Branching / PR rules; CI gates that must pass.
- Anything that differs from the global CLAUDE.md.

## Gotchas

- Known footguns, flaky areas, stale-process traps, env-specific data.
